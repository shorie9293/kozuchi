# $NAME — 現世の神勅

> 此の現世は高天原の全現世Feature-First構造令（AGENTS.md 1.3.4）に従う。

## 構造
Feature-First。新規Featureは `lib/features/<feature名>/` に創生せよ。

## 試練
```bash
export PATH="\$HOME/flutter/bin:\$PATH"
flutter analyze
flutter test --no-pub
```
